package com.marmoush.scalasamples.samples

object ExColl {

  def main(args: Array[String]) {

    /**
    * withFilter only delimits the result to the next calling function,
    * but filter creates a totally new collection
    */

  }
}
